import type { Span } from "@opentelemetry/api";
import type { SimpleSpanProcessor } from "@opentelemetry/sdk-trace-base";
import type { TraceFormatSpans } from "../core/type.js";
import { type AIGNEObserverOptions } from "../core/type.js";
export declare class AIGNEObserver {
    private storage?;
    tracer: import("@opentelemetry/api").Tracer;
    processor: SimpleSpanProcessor | undefined;
    private sdkServerStarted;
    static exportFn?: (spans: TraceFormatSpans[]) => Promise<void>;
    static setExportFn(exportFn: (spans: TraceFormatSpans[]) => Promise<void>): void;
    constructor(options?: AIGNEObserverOptions);
    serve(): Promise<void>;
    _serve(): Promise<void>;
    flush(span: Span): Promise<void>;
    close(): Promise<void>;
}
