import type { Server } from "node:http";
import type express from "express";
import { type StartServerOptions } from "./base.js";
export declare function startObservabilityBlockletServer(options: StartServerOptions): Promise<{
    app: express.Express;
    server: Server;
}>;
