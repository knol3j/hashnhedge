#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/bunwrapper$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/bunwrapper$exe"  "$basedir/../@aigne/example-mcp-github/index.ts" $args
  } else {
    & "$basedir/bunwrapper$exe"  "$basedir/../@aigne/example-mcp-github/index.ts" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "bunwrapper$exe"  "$basedir/../@aigne/example-mcp-github/index.ts" $args
  } else {
    & "bunwrapper$exe"  "$basedir/../@aigne/example-mcp-github/index.ts" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
